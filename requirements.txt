# ── Core Framework ──────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=13.0
pydantic>=2.0
pydantic-settings>=2.0

# ── AI — Face Recognition ──────────────────────────────
# deepface v0.0.99 is vendored at vendor/deepface/ (extracted from tarball)
# Install via: pip install ./vendor/deepface  (handled in Dockerfile)
opencv-python-headless>=4.10.0
tf-keras                            # DeepFace dependency (lightweight)
# mediapipe is a transitive dep of DeepFace (detector_backend="mediapipe")

# ── AI — LLM Providers ─────────────────────────────────
# openai client talks to Claude (via Anthropic), GPT-4o, AND Ollama
anthropic>=0.80.0                   # Claude Sonnet (conversation)
openai>=1.50.0                      # GPT-4o (puzzle gen) + Ollama (extraction)

# ── AI — Jailbreak Detection ──────────────────────────
onnxruntime>=1.17.0                 # DeBERTa + DistilBERT local inference
transformers>=4.40.0

# ── AI — Embeddings ────────────────────────────────────
sentence-transformers>=3.0.0        # BGE-M3 (1024-dim, local)

# ── Vector Database ────────────────────────────────────
pymilvus>=2.4.0                     # Milvus Lite (local file, no server)

# ── Knowledge Graph ────────────────────────────────────
networkx>=3.3

# ── Data & Security ────────────────────────────────────
# sqlcipher3-binary>=0.5.0          # macOS only
keyring>=25.0.0
cryptography>=43.0.0

# ── macOS Hardware Access ──────────────────────────────
# pyobjc-core>=10.0                 # macOS only
# pyobjc-framework-IOKit>=10.0      # macOS only

# ── Utilities ──────────────────────────────────────────
pillow>=10.0.0
numpy>=1.26.0
python-dotenv>=1.0.0

# ── Build & Packaging (production only) ───────────────
# cython>=3.0.0                     # Compile security modules to .so
# setuptools>=70.0.0                # Required by Cython build
# pyinstaller>=6.0.0                # Single-folder macOS bundle

# ── TTS — Edge neural TTS ──────────────────────────────
edge-tts>=6.0.0                     # Microsoft Edge TTS — sub-second synthesis, no GPU/model needed

# ── Testing ────────────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0                       # For TestClient async tests
